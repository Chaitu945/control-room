# Normalise to LF in the repository. Without this, Windows checkouts with
# core.autocrlf=true commit CRLF and CI diffs the whole tree.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary

package-lock.json -diff linguist-generated
